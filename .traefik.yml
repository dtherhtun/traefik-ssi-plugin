displayName: "SSI Plugin"
type: middleware
import: github.com/username/traefik-plugin-ssi
summary: "Server Side Includes middleware for Traefik"
testData:
  config:
    includeTimeoutSeconds: 5
    cacheTTLSeconds: 60
    maxConcurrency: 8
